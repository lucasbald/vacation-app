import React from "react";

import "./Header.css"

const Header = () => {
  return (
    <header className="header-container">
      <h1>Vacation App</h1>
    </header>
  );
};

export default Header;
